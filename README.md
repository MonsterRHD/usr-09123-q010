# 学校支持措施跟踪

本服务面向教师身心健康支持业务，负责学校支持措施跟踪相关的机器接口、业务状态和持久化记录。目录中的实现应保持后端边界，配置通过环境变量提供，敏感信息不写入版本库。

## 开发约定

接口以 JSON 或消息事件交换数据，领域状态变更需要记录时间、来源和操作者。运行前准备对应的数据服务，使用仓库中的依赖清单安装运行时依赖；自动化测试放在 test 或 tests 目录并通过标准命令执行。

